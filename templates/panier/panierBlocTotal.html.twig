<div class="card shadow-0 border">
  <div class="card-body">
    
    {# <div class="d-flex justify-content-between">
      <p class="mb-2">Total price:</p>
      <p class="mb-2">$329.00</p>
    </div>
    <div class="d-flex justify-content-between">
      <p class="mb-2">Discount:</p>
      <p class="mb-2 text-success">-$60.00</p>
    </div>
    <div class="d-flex justify-content-between">
      <p class="mb-2">TAX:</p>
      <p class="mb-2">$14.00</p>
    </div>
    <hr /> #}
    <div class="d-flex justify-content-between">
      <p class="mb-2">
        <b>Total</b> :<br />
      (Frais de livraison inclus)
        </p>
      <p class="mb-2 fw-bold">{{totalPanier}} €</p>
    </div>
    <input type="hidden" name="totalPanier" value="{{totalPanier}}" />

    <div id="errorAdresses" style="display:none;"></div>


    <div class="mt-3">
      {# <a href="{{path("app_paiement_index")}}" class="btn btn-success w-100 shadow-0 mb-2"> Je valide ma commande </a> #}
      <a id="aeValidate" onclick="verifAdresses();" class="btn btn-success w-100 shadow-0 mb-2"> Je valide ma commande </a>
      <a href="{{path("app_home")}}" class="btn btn-light w-100 border mt-2"> Je continue mon shopping </a>
    </div>
  </div>
</div>
