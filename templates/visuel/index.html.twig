{% extends 'base.html.twig' %}

{% block title %}Visuel index{% endblock %}

{% block body %}
    <h1>Visuel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CheminVisuel</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for visuel in visuels %}
            <tr>
                <td>{{ visuel.id }}</td>
                <td>{{ visuel.cheminVisuel }}</td>
                <td>
                    <a href="{{ path('app_visuel_show', {'id': visuel.id}) }}">show</a>
                    <a href="{{ path('app_visuel_edit', {'id': visuel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_visuel_new') }}">Create new</a>
{% endblock %}
